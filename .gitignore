node_modules
.env
cache
artifacts
cache-foundry
out-foundry

# Generated by scripts/deploy-local.js each time you redeploy locally —
# never a checked-in file, since the address/genesis hash change every run.
config.local.js

# Local hardhat node output, if you redirect it to a file when running
# `npm run local:node` in its own terminal — never a checked-in file.
.local-node.log
